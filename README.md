# PoMoDoRo

Приложение PoMoDoRo — простой и аккуратный Pomodoro‑таймер для повышения продуктивности: таймеры фокусной работы, короткие и длинные перерывы, история сессий, настраиваемые параметры и приятная ambient‑музыка во время работы.

Кому подходит
- Студентам, специалистам и всем, кто хочет структурировать время и улучшить концентрацию.

Краткое описание работы
- Настраиваемые длительности Pomodoro / Short Break / Long Break.
- Ambient‑музыка во время активной сессии (опционально) и звуковой сигнал по окончании.
- История завершённых сессий сохраняется локально (Room DB).
- Настройки, где можно выбрать трек для ambient и мелодию окончания.

Что нового в этом обновлении (v1.1)
- Добавлена возможность настраивать длительность таймера напрямую в настройках приложения.
- Добавлена ambient‑музыка: пользователь может выбрать mp3‑трек из пакета ресурсов, музыка проигрывается в цикле во время работы таймера и автоматически останавливается при паузе/остановке.
- Добавлен звук окончания: можно выбрать мелодию из ресурсов или использовать системный звук.
- Обновлён UI: более аккуратные карточки, современные кнопки и адаптивный фон.

Быстрый старт (сборка и запуск)
Требования
- Windows 10/11 (или macOS/Linux) с Android Studio и JDK 11+.
- Android SDK (соответствующие платформы) и эмулятор или физическое устройство.

Сборка и запуск (PowerShell)
```powershell
cd C:\Users\STRNGBBY\AndroidStudioProjects\PoMoDoRo
# собрать apk
./gradlew assembleDebug
# установить на устройство
./gradlew installDebug
```

Техническое задание (полная версия во вложенном файле `TECHNICAL_SPEC.md`)
В корне репозитория добавлен подробный технический документ `TECHNICAL_SPEC.md`, где описаны: цель приложения, функциональные и нефункциональные требования, архитектура, модель данных, сценарии использования, требования к фоновому выполнению таймера, приёмочные критерии, план тестирования и рекомендации по CI/CD.

Как помочь проекту
- Добавлять issues с багами и предложениями фич.
- Оставлять pull‑request'ы с описанием изменений.
- Писать тесты для новых функций и исправлений.

Как запушить проект на GitHub (напоминание)
1. Создайте репозиторий на GitHub (через веб-интерфейс или с помощью `gh`):
   - Через веб: https://github.com/new
   - Через gh CLI: `gh repo create USER/REPO --public`

2. В корне проекта выполните (PowerShell):

```powershell
cd C:\Users\STRNGBBY\AndroidStudioProjects\PoMoDoRo
# настроить имя и email, если ещё не настроены
git config user.name "Ваше Имя"
git config user.email "you@example.com"

# инициализировать репозиторий
git init
git add .
git commit -m "Initial commit"

# указать основную ветку и добавить remote (замените USER/REPO)
git branch -M main
# HTTPS
git remote add origin https://github.com/USER/REPO.git
# или SSH
# git remote add origin git@github.com:USER/REPO.git

git push -u origin main
```

Аутентификация
- При использовании HTTPS потребуется Personal Access Token (PAT) вместо пароля. Создайте PAT на https://github.com/settings/tokens и используйте его при запросе пароля.
- При использовании SSH — добавьте ваш публичный ключ (обычно `~/.ssh/id_rsa.pub`) в GitHub Settings -> SSH and GPG keys.

Контакт и вклад
Если хотите, чтобы я внёс дополнительные изменения (добавить CI, оформить badges, добавить LICENSE или .gitignore) — скажите, и я сделаю это автоматически.
